# Generated by Shade Framework - DO NOT EDIT
# Source: shade.yml + docker-compose.yml

services:
  vllm:
    container_name: vllm
    image: vllm/vllm-openai-cpu:v0.17.1
    volumes:
    - model-store:/models
    entrypoint:
    - /bin/sh
    - -c
    command:
    - "echo \"Waiting for model push...\"\nwhile ! curl -sf http://model-service:8001/ready;\
      \ do sleep 5; done\necho \"Model ready, starting vLLM\"\nexec python3 -m vllm.entrypoints.openai.api_server\
      \ \\\n  --model /models/current \\\n  --host 0.0.0.0 \\\n  --max-model-len 512\
      \ \\\n  --max-num-seqs 1 \\\n  --swap-space 0 \\\n  --enforce-eager \\\n  --disable-frontend-multiprocessing\
      \ \\\n  --enable-log-requests \\\n  --served-model-name Qwen/Qwen2.5-0.5B-Instruct\n"
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8000/health
      start_period: 10m
      start_interval: 30s
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
    - proxy
  model-service:
    container_name: model-service
    image: ghcr.io/concrete-security/private-model-serving-model-service@sha256:4ed581c089bb5af4862e73f4e72ed71a110d07fe3817af3f8da6d68a8cf67068
    volumes:
    - model-store:/models
    environment:
      MODEL_DIR: /models/current
      PUSH_TOKEN: ${PUSH_TOKEN:-}
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - uv
      - run
      - python
      - -c
      - import urllib.request; urllib.request.urlopen('http://localhost:8001/health')
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
    - proxy
  nginx-cert-manager:
    image: ghcr.io/concrete-security/shade-cert-manager:sha-339842a
    container_name: nginx-cert-manager
    ports:
    - 80:80
    - 443:443
    environment:
    - DOMAIN=model-serving.concrete-security.com
    - CORS_ORIGINS=["^https://.*$$"]
    - AUTH_ENABLED=false
    - DEV_MODE=false
    - LETSENCRYPT_STAGING=false
    - LETSENCRYPT_ACCOUNT_VERSION=v1
    - FORCE_RM_CERT_FILES=false
    - LOG_LEVEL=INFO
    - NGINX_MAX_BODY_SIZE=10G
    - "EXTRA_UPSTREAMS=upstream model_service_8001 {\n    server model-service:8001;\n\
      }\n\nupstream vllm_8000 {\n    server vllm:8000;\n}"
    - "EXTRA_LOCATIONS=    location /push-model {\n$${CORS_HEADERS}\n        proxy_pass\
      \ http://model_service_8001;\n        proxy_set_header Host $$host;\n      \
      \  proxy_set_header X-Real-IP $$remote_addr;\n        proxy_set_header X-Forwarded-For\
      \ $$proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $$scheme;\n\
      \    }\n\n    location /model-hash {\n$${CORS_HEADERS}\n        proxy_pass http://model_service_8001;\n\
      \        proxy_set_header Host $$host;\n        proxy_set_header X-Real-IP $$remote_addr;\n\
      \        proxy_set_header X-Forwarded-For $$proxy_add_x_forwarded_for;\n   \
      \     proxy_set_header X-Forwarded-Proto $$scheme;\n    }\n\n    location /v1/\
      \ {\n$${CORS_HEADERS}\n        proxy_pass http://vllm_8000;\n        proxy_set_header\
      \ Host $$host;\n        proxy_set_header X-Real-IP $$remote_addr;\n        proxy_set_header\
      \ X-Forwarded-For $$proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto\
      \ $$scheme;\n    }"
    volumes:
    - tls-certs-keys:/etc/nginx/ssl/
    - /var/run/dstack.sock:/var/run/dstack.sock
    networks:
    - proxy
    - attestation
    restart: unless-stopped
  attestation-service:
    image: ghcr.io/concrete-security/shade-attestation-service:sha-339842a
    container_name: attestation-service
    environment:
    - HOST=0.0.0.0
    - PORT=8080
    - WORKERS=8
    volumes:
    - /var/run/dstack.sock:/var/run/dstack.sock
    expose:
    - '8080'
    networks:
    - attestation
    restart: unless-stopped
    deploy:
      mode: replicated
      replicas: 1
networks:
  proxy:
    driver: bridge
  attestation:
    driver: bridge
volumes:
  tls-certs-keys: null
  model-store: null
